<%= form_for(@seance_laboratoire) do |f| %>
  <% if @seance_laboratoire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seance_laboratoire.errors.count, "error") %> prohibited this seance_laboratoire from being saved:</h2>

      <ul>
      <% @seance_laboratoire.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :number %><br>
    <%= f.text_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
